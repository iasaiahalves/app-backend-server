.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Swap the comments on the following lines if you wish to use zero-installs
# In that case, don't forget to run `yarn config set enableGlobalCache false`!
# Documentation here: https://yarnpkg.com/features/caching#zero-installs

#!.yarn/cache
.pnp.*

# Node.js dependencies (just in case)
node_modules/

# Environment variables (prevents exposing secrets)
.env

# Uploads folder (if you don’t want uploaded images stored in GitHub)
uploads/

# Log files (to avoid unnecessary commits)
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor & OS files (for cleanliness)
.vscode/
.DS_Store
Thumbs.db
